// Package task defines task runner, subagent, delegation, and stream contracts
// for the Agent SDK.
//
// The root package owns task identity, state, snapshots, observers, start and
// control requests, storage, result persistence, and management. Focused
// delegation, stream, and subagent contracts live in their subpackages.
package task
