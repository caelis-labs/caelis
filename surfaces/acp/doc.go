// Package acp exposes Caelis as an ACP Surface over stdio. It adapts the
// aggregate AppServer client boundary into ACP sessions, prompts, presentation,
// terminal, and Task observation without owning Control or Runtime state.
package acp
