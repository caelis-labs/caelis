// Package memoryhost owns Host-private composition for a verified, managed
// local Memory appliance and its pure public SDK clients. It never reads the
// appliance database or interprets Memory domain state.
package memoryhost
