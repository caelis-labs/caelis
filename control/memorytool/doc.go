// Package memorytool owns the Caelis model projection for the two Memory
// Appliance operations: remember(text) and recall(query). All identity,
// audience, capability, consistency, source, and budget controls stay hidden.
package memorytool
