// Package runtime defines the standalone agent/runtime contracts for the next
// Caelis SDK. Root package files contain only public contracts and small
// immutable helper implementations. Concrete runtimes live in subpackages.
package runtime
